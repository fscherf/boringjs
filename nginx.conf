events {}

http {
	server {
		listen 80;
		server_name _;

		root /var/www;

		location /static/ {
			try_files $uri =404;
		}

		location /templates/ {
			try_files $uri =404;
		}

		location / {
			try_files $uri $uri/ /index.html;
		}
	}
}
